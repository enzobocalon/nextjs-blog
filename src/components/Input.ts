import styled from 'styled-components';

export const Input = styled.input`
  padding: 1rem;
  border: none;
  outline: none;
  border-radius: .25rem;
  background-color: white;
  color: white;
  font-size: 1rem;
  font-family: inherit;
`;
